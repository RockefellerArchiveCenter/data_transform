[tox]
envlist = py312, linting
skipsdist = True

[testenv]
skip_install = True
deps =
  -rrequirements.txt
  -rtests/test_requirements.txt

[testenv:py312]
setenv =
  ENVIRONMENT=dev
  AWS_REGION=us-east-1
  AWS_SSM_ROLE_ARN=arn:aws:iam::123456789:role/ssm-role
  APP_CONFIG_PATH=data_transform
commands =
	coverage run -m pytest -s
	coverage report -m

[testenv:linting]
basepython = python3
deps = pre-commit
commands =
    pre-commit run --all-files

[flake8]
application-import-names = flake8
select = B, C, E, F, W, B950
import-order-style = pep8
max-complexity = 10
ignore =
  # Line too long (82 > 79 characters).
	E501,
  # Blank line at end of file
	W391,
  # Boolean operator at the start of a line
  W503